<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notificationlistener3">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".MyNotificationListenerService"
            android:label="NotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity android:name=".FocusModeActivity"></activity>
        <activity android:name=".RestTimeActivity"></activity>
        <activity android:name=".SettingMainActivity"></activity>

        <activity android:name=".SettingNotificationActivity"
            android:parentActivityName=".SettingMainActivity"></activity>

        <activity android:name=".TimeSettings"
            android:parentActivityName=".SettingMainActivity" >
        </activity>

        <activity android:name=".LampSettings"
            android:parentActivityName=".SettingMainActivity"></activity>
    </application>

</manifest>